# 偏差与决策日志

## 使用规则
- 所有偏离原计划的调整都必须登记。
- 所有允许带遗留项进入下一阶段的决定都必须登记。
- 所有 blocker 与 high 风险处置方案都必须登记。

## 字段模板
| 日期 | 阶段 | 类型 | 决策/偏差 | 原因 | 影响 | Owner | Reviewer | Validator | 后续动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 当前记录
| 日期 | 阶段 | 类型 | 决策/偏差 | 原因 | 影响 | Owner | Reviewer | Validator | 后续动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-03-14 | Phase 0 | 初始化 | 建立 `docs/refactor/` 重构文档体系并采用严格门禁 | 避免超大型项目在上下文切换中丢失阶段边界和验证依据 | 后续阶段必须同步更新控制面板与验证看板 | 待指定 | 待指定 | 待指定 | 开始落地 Phase 0 基线与资产盘点 |
| 2026-03-14 | Phase 0 | 风险确认 | RN 首开基线当前受元素级自动化识别能力不足阻塞 | `ReactNativeJNI` 与 `uiautomator dump` 无法稳定给出根宿主页元素证据 | RN 根宿主页白屏保留为高风险遗留，但不阻塞 Phase 0 关闭 | 待指定 | 待指定 | 待指定 | 后续以 release-like 或专项调试方式补采 |
| 2026-03-15 | Phase 0 | 决策 | 采用“书架”作为 RN 宿主页代表首开样本 | 书架与分类同属 RN 页面，且书架在冷启动后首次进入时具备稳定正向渲染截图和日志证据 | `V0-03` 获得可替代的 RN 正向样本，`profile` 白屏降为高风险遗留 | 待指定 | 待指定 | 待指定 | Phase 0 关闭评审时明确带入下一阶段的条件 |
| 2026-03-15 | 阶段切换 | 决策 | 按项目推进指令将 Phase 0 视为已通过并启动 Phase 1 | Phase 0 已形成完整关闭评审材料与绿色验证项 | 控制面板和执行重心切换到 `Phase 1 in_progress` | 待指定 | 待指定 | 待指定 | 开始执行发布、安全与合规治理 |
| 2026-03-15 | Phase 1 | 进展 | 启动权限最小化收口 | 清理 Manifest 中普通应用不应保留的权限，并对旧版电话权限范围做限制 | `V1-03` 进入 `in_progress` | 待指定 | 待指定 | 待指定 | 后续补权限矩阵说明 |
| 2026-03-15 | Phase 1 | 进展 | 启动 endpoint 配置收口脚手架 | 后端 API 和图片 host 散落在 Kotlin 代码中，不利于 release 安全治理 | `V1-02` 进入 `in_progress`，地址统一收口到 `BuildConfig` | 待指定 | 待指定 | 待指定 | 后续补环境注入和 release/debug 分流 |
| 2026-03-15 | Phase 1 | 进展 | 启动 cleartext 策略拆分 | 主资源此前对所有构建类型全局放开 cleartext，不符合发布路径目标 | `V1-04` 进入 `in_progress`，主资源默认关闭 cleartext，debug 单独放开 | 待指定 | 待指定 | 待指定 | 后续对白名单域名继续精细化 |
| 2026-03-15 | Phase 1 | 进展 | 启动 Room 发布路径治理第一步 | 当前数据库 `exportSchema=false` 且 destructive migration 默认开启 | `V1-05` 进入 `in_progress`，schema 导出开启，destructive migration 仅保留给 debug | 待指定 | 待指定 | 待指定 | 后续补迁移策略与演练矩阵 |
| 2026-03-15 | Phase 1 | 进展 | 启动福利页 WebView 安全收口 | 福利页默认 URL 为硬编码 `http`，外链与错误处理存在未完成分支 | `V1-07` 进入 `in_progress`，白名单、外链浏览器打开、默认 URL 配置化开始落地 | 待指定 | 待指定 | 待指定 | 真机验证后继续补异常分支 |
| 2026-03-15 | Phase 1 | 进展 | 将福利页默认测试站点切换为 Bing | 原福利测试地址不可用，影响真机验证 | `V1-07` 的运行验证基准页切换到 `https://www.bing.com/` | 待指定 | 待指定 | 待指定 | 补真机截图与跳转证据 |
| 2026-03-15 | Phase 1 | 进展 | 已完成 Bing 福利页真机运行取证 | 真机日志显示福利页跳转到 `https://cn.bing.com/` 且 `OnPageFinished` 成功触发 | `V1-07` 获得首轮正向运行证据 | 待指定 | 待指定 | 待指定 | 后续可继续补外链与异常路径 |
| 2026-03-15 | Phase 1 | 进展 | 启动 release 生产化脚手架 | 当前 release 仍以接近调试态配置为主，未真正打开压缩与收缩 | `V1-01` 进入 `in_progress` | 待指定 | 待指定 | 待指定 | 继续用 release 构建暴露真实发布风险 |
| 2026-03-15 | Phase 1 | 进展 | release 产物已首次成功构建 | 经过 R8 缺类规则补齐和 network security lint 修正后，`assembleRelease` 成功通过 | `V1-01` 具备正向验证证据，产物与 mapping 已生成 | 待指定 | 待指定 | 待指定 | 后续继续细化签名、资源收缩收益和发布矩阵 |
| 2026-03-15 | Phase 1 | 进展 | 启动 release 签名配置脚手架 | release 仍沿用 debug 签名，缺少正式签名参数读取结构 | `V1-08` 进入 `in_progress`，支持从 Gradle 属性和环境变量读取签名参数 | 待指定 | 待指定 | 待指定 | 后续补签名说明与 CI 注入规范 |
| 2026-03-15 | Phase 1 | 进展 | 已生成 Gradle 依赖校验元数据 | 供应链治理需要可追溯的依赖校验基础 | `V1-08` 从“只有签名脚手架”推进到“签名脚手架 + Gradle 校验” | 待指定 | 待指定 | 待指定 | 后续继续补 CI 注入约定与 npm 风险登记 |
| 2026-03-15 | Phase 1 | 进展 | 补齐迁移演练矩阵和首轮实操样本 | 当前数据库、SharedPreferences、KeyChain 的发布路径已开始收口，但缺少统一演练矩阵会导致升级风险不可系统评估 | `V1-06` 已具备矩阵和首轮“重装保留数据”实操证据 | 待指定 | 待指定 | 待指定 | 后续继续补 token 样本与旧 schema 升级演练 |
| 2026-03-15 | Phase 1 | 进展 | 启动并验证 benchmark 环境 | macrobenchmark 模块存在 `uiautomator:<latest>` 这类不稳定依赖声明，且缺少统一环境说明 | `V1-09` 已完成版本固定、环境说明和 `:macrobenchmark:assemble` 构建验证 | 待指定 | 待指定 | 待指定 | 后续在真实设备上补 `connectedCheck` 或 Baseline Profile 生成证据 |
| 2026-03-15 | Phase 1 | 决策 | Phase 1 进入关闭评审准备状态 | 当前 `V1-01 ~ V1-09` 已全部具备绿色证据 | 控制面板、阶段文档与关闭评审文档进入 `ready_for_validation` | 待指定 | 待指定 | 待指定 | 下一步完成 Phase 1 关闭验证并启动 Phase 2 |
| 2026-03-15 | 阶段切换 | 决策 | 按项目推进指令将 Phase 1 视为已通过并启动 Phase 2 | Phase 1 已形成完整关闭评审材料与全部绿色验证项 | 控制面板、阶段文档与执行重心切换到 `Phase 2 in_progress` | 待指定 | 待指定 | 待指定 | 开始执行 JVM 单测基础设施任务 |
| 2026-03-15 | Phase 2 | 进展 | JVM 单测基础设施已落地并完成首轮执行 | `src/test`、测试依赖和首批纯逻辑测试已落地，且 `app:testDebugUnitTest` 已成功通过 | `V2-01` 与 `V2-02` 已具备待验证证据 | 待指定 | 待指定 | 待指定 | 下一步继续补 `V2-03` fixture/fake data 层 |
| 2026-03-15 | Phase 2 | 进展 | fixture/fake data 基础层已落地 | 已将首批样本放入 `src/test/resources/fixtures/`，并提供 `FixtureCatalog` 与 `FakeReaderHistorySource` 作为测试数据入口 | `V2-03` 已具备待验证证据，可继续被 reducer/usecase/repository 测试复用 | 待指定 | 待指定 | 待指定 | 下一步推进 Bridge contract tests 或第二批 JVM 测试 |
| 2026-03-15 | Phase 2 | 进展 | Bridge contract tests 已落地 | 为了在不改动业务逻辑和 UI 的前提下拦截 Native/RN 协议回归，先补齐 UserBridge/NavigationBridge Promise 合同以及 ThemeChanged/WritePageSelectionMenuAction 事件合同 | `V2-04` 进入 `ready_for_validation`，后续可继续推进 smoke 或 CI 门禁 | 待指定 | 待指定 | 待指定 | 下一步优先评估 `V2-05` smoke 套件或 `V2-06` CI 工作流 |
| 2026-03-15 | Phase 2 | 阻塞 | smoke 套件基础层已落地但真机执行被 ADB 环境阻塞 | 已新增首页/登录/搜索/阅读器 Android smoke 与设置 RN smoke，且 RN smoke 与 `compileDebugAndroidTestKotlin` 已通过；但 `connectedDebugAndroidTest` 执行时 `adb devices` 返回空列表 | `V2-05` 目前只能维持 `blocked/yellow`，待当前 shell 识别到设备后补真机证据即可继续关闭 | 待指定 | 待指定 | 待指定 | 重新连接 ADB 设备后重跑 `connectedDebugAndroidTest`，补齐录屏/截图与通过结果 |
| 2026-03-16 | Phase 2 | 进展 | Android/RN smoke 套件已补齐真机与本地证据 | 当前 shell 已重新识别到设备 `192.168.8.130:5555`，随后完成 `connectedDebugAndroidTest` 真机执行；同时再次确认 `SettingsPage.smoke.test.tsx` 通过 | `V2-05` 进入 `ready_for_validation`，Phase 2 的核心路径 smoke 已形成可重复执行证据链 | 待指定 | 待指定 | 待指定 | 后续将 smoke job 在 CI emulator 上稳定化后再考虑升级为 blocking |
| 2026-03-16 | Phase 2 | 进展 | Phase 2 已进入关闭评审准备态 | 核心质量资产已到位：JVM tests、fixture、Bridge contract、smoke、workflow、证据归档、flake 规则、PR 门禁矩阵均已落地并同步看板 | 控制面板状态切换为 `ready_for_validation`，后续将以 Phase 3 前的关闭评审结论作为正式阶段切换依据 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 进入 Phase 2 关闭评审与后续结构重构准备 |
| 2026-03-15 | Phase 2 | 阻塞 | CI 本地基线已部分跑通，但仍有 lint/detekt 历史债务 | 已补齐 Jest AsyncStorage/reanimated/native UI mocks，`npm test -- --runInBand` 转绿；同时修复 `StateAdapter` 与 `NetworkMonitor` 使 `lintDebug` 通过。Detekt 已从配置阻塞恢复到真实扫描阶段，但当前仍报 `2260 weighted issues`；`npm run lint` 仍暴露大量现存 RN lint debt | `V2-06` 当前可先承接通过的 build/test/lint 子命令，完整阻断型 CI 仍需继续收口 RN lint 与 detekt 代码债务 | 待指定 | 待指定 | 待指定 | 下一步优先决定 detekt/RN lint 采用全量清债还是增量门禁，再落 workflow 阻断策略 |
| 2026-03-15 | Phase 2 | 进展 | 首版 quality workflow 已落地 | 已新增 `.github/workflows/quality-gates.yml`，将本地已验证通过的命令接入 blocking job，同时把 `android-smoke`、`rn-lint`、`app:detekt` 以 observe job 形式上线 | 项目已不再只有 label workflow，`V2-06` 从“无质量工作流”推进到“有阻断 + 有观测”的过渡态 | 待指定 | 待指定 | 待指定 | 后续在 CI 实际运行结果基础上，逐步把 observe job 升级为 blocking |
| 2026-03-15 | Phase 2 | 进展 | 证据归档标准已固定 | 已新增 benchmark diff、size diff、smoke 证据的统一命名、字段、目录和 markdown 模板标准，避免后续出现截图/录屏不可追溯 | `V2-08` 进入 `ready_for_validation`，后续所有 Phase 2 证据需按统一格式归档 | 待指定 | 待指定 | 待指定 | 后续新增 benchmark/size/smoke 证据时强制套用该标准 |
| 2026-03-15 | Phase 2 | 进展 | flake 处理规则已形成初稿 | 已固定 flake 定义、重试上限、阻断策略、升级规则、隔离条件与角色模板，避免后续 CI 通过无限重跑掩盖真实问题 | `V2-09` 已进入规则可执行阶段，但仍需在 CI 实跑后补具体责任人和实际案例 | 待指定 | 待指定 | 待指定 | 后续在 CI 首轮运行结果出来后补充 flake 实例与责任人 |
| 2026-03-16 | Phase 2 | 决策 | 采用“blocking + observe”增量门禁策略关闭 Phase 2 | 当前已有一组本地稳定通过的 build/test/lint 命令，可直接用于阻断 PR；同时 `rn-lint` 与 `detekt` 仍存在大规模历史债，若强行全量阻断会直接压垮当前节奏 | `V2-06` 与 `V2-07` 进入 `ready_for_validation/green`，并把历史债相关 job 保持在线观测，后续逐步升级为 blocking | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 进入 Phase 2 关闭评审，并将下一阶段的清债/结构重构放到后续 roadmap |
| 2026-03-16 | Stage 2 Planning | 决策 | 第二阶段范围固定为 `Phase 3 + Phase 4` | 第一阶段已完成基线、发布安全与质量门禁，下一步最适合先做基础设施收口与边界收口，不直接进入真正 Gradle 模块化 | 第二阶段正式计划、Phase 3/4 详细文档、验证看板与静态债基线文档已落盘到 `docs/refactor/`，供后续实施直接使用 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 待第一阶段正式切换为 `validated` 后启动第二阶段 |
| 2026-03-16 | 阶段切换 | 决策 | 第一阶段（Phase 0-2）正式关闭为 `validated` | `V0-*`、`V1-*`、`V2-*` 均已形成绿色证据链，且 `Phase 2 closeout assessment` 已给出建议通过结论 | 允许正式进入第二阶段，控制面板与阶段状态切换到 `Phase 3 in_progress` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 开始执行 `P3.1 / P3.2` 之后的基础设施收口主线 |
| 2026-03-16 | Phase 3 | 进展 | 旧网络壳真实调用矩阵已落盘 | 已对 `ApiService / RetrofitClient` 的调用分布做主路径分级，并明确高风险生产路径、次级兼容路径与初始化依赖路径的迁移顺序 | `V3-01` 从 `planned` 推进到 `in_progress`，后续可直接进入 `NetworkFacade + LegacyApiServiceAdapter` 收口实施 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步开始建立 `NetworkFacade` 与旧壳兼容适配层 |
| 2026-03-16 | Phase 3 | 进展 | `NetworkFacade + LegacyApiServiceAdapter` 骨架已落地 | 已新增统一网络抽象、旧 `ApiService` 兼容适配层与配套 JVM 单测，且 `app:testDebugUnitTest --tests com.novel.core.network.LegacyApiServiceAdapterTest` 与 `app:testDebugUnitTest` 均已通过 | `P3.4` 已具备继续接入高风险生产路径的最小实现基础 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步开始迁移 Home / Search / Bridge 高风险调用到主网络抽象 |
| 2026-03-16 | Phase 3 | 进展 | 首个高风险 Bridge 调用已迁入 `NetworkFacade` 主通路 | `NavigationBridgeModule.getHomeBooksHighPriority()` 属于高风险 Bridge 生产路径，适合作为 `P3.5` 的最小切口；为保证本地 JVM 单测稳定运行，网关 JSON 解析采用 `Gson JsonParser` 而不是 Android `org.json` | `V3-01` 获得首个真实迁移样本，`NavigationBridgeNetworkGatewayTest` 与 `app:testDebugUnitTest` 已通过；当前主通路唯一化仍未完成，后续需继续迁移 Home / Search / 其他 Bridge 路径 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步扩展 `NetworkFacade` 到 Search / Home 其余高风险调用，并继续保持原子化提交 |
| 2026-03-16 | Phase 3 | 进展 | 分类与搜索 Bridge 调用已迁入 `NetworkFacade` 主通路 | `NavigationBridgeModule` 仍是当前最优先高风险边界，`getBookCategories()` 与 `searchBooks()` 与已迁移的首页推荐共享前台主链路和相同 `front` 主域名，适合继续在同一原子主题内收口 | `V3-01` 从“单一样本”推进到“首页推荐 + 分类 + 搜索”三条前台 Bridge 路径接入统一网关，且 `app:testDebugUnitTest --tests com.novel.rn.bridge.network.NavigationBridgeNetworkGatewayTest` 与 `app:testDebugUnitTest` 已通过；当前作者路径与服务层高风险调用仍待迁移 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步优先处理 `NavigationBridgeModule.getAuthorBooks()` 或按迁移矩阵进入 `HomeService/SearchService` |
| 2026-03-16 | Phase 3 | 进展 | 作者作品 Bridge 调用已迁入 `NetworkFacade` 主通路，`NavigationBridgeModule` 旧 GET 直连清零 | `getAuthorBooks()` 是 `NavigationBridgeModule` 内最后一个旧 `ApiService.get()` 调用；将其一并收口后，可以把高风险 Bridge 边界与服务层高风险路径明确分开治理 | `V3-01` 已从“前台 Bridge 样本迁移”推进到“`NavigationBridgeModule` 四条网络路径全部接入统一网关”，且 `NavigationBridgeNetworkGatewayTest` 与 `app:testDebugUnitTest` 持续通过；后续收口重点转向 `HomeService / SearchService / UserService` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按迁移矩阵进入 `HomeService` 或 `SearchService`，继续保持单主题原子提交 |
| 2026-03-16 | Phase 3 | 进展 | `HomeService` 已迁入 `NetworkFacade` 主通路 | `HomeService` 在迁移矩阵中位于 `NavigationBridgeModule` 之后，且其对外使用面目前主要集中在阻塞版方法，适合作为首个服务层高风险收口样本 | `V3-01` 已从“Bridge 边界清理”推进到“Bridge + HomeService 首批服务层路径清理”，`HomeServiceTest`、`app:testDebugUnitTest --tests com.novel.utils.network.api.front.HomeServiceTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `SearchService / UserService` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按迁移矩阵进入 `SearchService`，继续保持单主题原子提交 |
| 2026-03-16 | Phase 3 | 进展 | `SearchService` 已迁入 `NetworkFacade` 主通路 | `SearchService` 在迁移矩阵中与 `HomeService` 同属首批服务层高风险路径，且外部主要消费阻塞版搜索接口，适合在 `HomeService` 之后继续原子化迁移 | `V3-01` 已从“Bridge + HomeService”推进到“Bridge + HomeService + SearchService” 主路径接入统一抽象，`SearchServiceTest`、`app:testDebugUnitTest --tests com.novel.utils.network.api.front.SearchServiceTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `UserService` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按迁移矩阵进入 `UserService`，继续保持单主题原子提交 |
| 2026-03-16 | Phase 3 | 进展 | `UserService` 认证核心路径已迁入 `NetworkFacade` 主通路 | `UserService` 调用面较大，不适合一次性全量迁移；先收口 `login/register/getUserInfo` 三条认证主路径，可以在不扩大影响面的前提下验证 `POST + GET` 混合场景 | `V3-01` 已从“Bridge + Home + Search”推进到“Bridge + Home + Search + User 认证主路径”接入统一抽象，`UserServiceAuthTest`、`app:testDebugUnitTest --tests com.novel.utils.network.api.front.user.UserServiceAuthTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `UserService` 剩余评论/反馈/书架路径 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续拆解 `UserService` 剩余高风险调用，保持单主题原子提交 |
| 2026-03-16 | Phase 3 | 进展 | `UserService` 查询型路径已迁入 `NetworkFacade` 主通路 | 在认证主路径完成后，优先收口 `getUserComments/getBookshelfStatus` 两条 GET 查询链路，可以延续相同抽象而不引入写操作副作用风险 | `V3-01` 已从“Bridge + Home + Search + User 认证”推进到“Bridge + Home + Search + User 认证/查询主路径”接入统一抽象，`UserServiceQueryTest`、`app:testDebugUnitTest --tests com.novel.utils.network.api.front.user.UserServiceQueryTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `UserService` 写操作路径 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步迁移 `UserService` 的评论/反馈/资料更新写操作，继续保持单主题原子提交 |
| 2026-03-17 | Phase 3 | 进展 | `UserService` 的真实写操作核心路径已迁入 `NetworkFacade` 主通路 | `updateUserInfoBlocking/postCommentBlocking` 仍属于当前实际调用面的写路径，适合作为 `UserService` 写操作收口的最小闭环；同时原文件中已无调用方的旧私有写接口可以直接清理，降低双轨维护风险 | `V3-01` 已从“Bridge + Home + Search + User 认证/查询”推进到“Bridge + Home + Search + User 认证/查询/真实写路径”接入统一抽象，`UserServiceMutationTest`、`app:testDebugUnitTest --tests com.novel.utils.network.api.front.user.UserServiceMutationTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `AppError` 或 `StorageFacade` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步评估 `UserService` 剩余无调用遗留接口是否直接删除归档，并切入 `AppError` 第一批统一落地 |
| 2026-03-17 | Phase 3 | 进展 | `AppError + DataResult<T>` 基础模型已落地 | `P3.8` 需要先有统一错误类型和结果容器，才能逐步接入 Home / Search / Bridge / Settings；先提交骨架和测试，可以把后续边界接入拆成更小的原子提交 | `V3-04` 已从 `planned` 推进到 `in_progress`，`AppErrorTest` 与 `app:testDebugUnitTest` 已通过；后续需要选择首个高风险边界把异常映射接入到新模型 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步选择 Home / Search / Bridge / Settings 之一，开始接入 `AppError` |
| 2026-03-17 | Phase 3 | 进展 | `AppError` 已接入 Home / Search 服务边界 | 为了不改变 UI 与业务语义，先在服务层新增 `DataResult` 返回接口，再由上层按需渐进接入，能把错误模型统一与行为稳定性解耦推进 | `V3-04` 已从“只有基础模型”推进到“基础模型 + Home/Search 服务边界接入”，`HomeServiceTest`、`SearchServiceTest` 定向测试与 `app:testDebugUnitTest` 已通过；后续重点转向 Bridge 或 Settings 边界 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续选择 Bridge 或 Settings 边界接入 `AppError`，保持单主题原子提交 |
| 2026-03-17 | Phase 3 | 进展 | `AppError` 已接入 Settings Bridge 错误映射 | Settings 属于 `V3-04` 指定的首批边界之一，且当前模块存在大量重复的超时/异常 `promise.reject` 分支；先引入统一映射器可以在不改变接口语义的前提下收敛错误处理方式 | `V3-04` 已从“Home/Search 服务接入”推进到“Home/Search + Settings Bridge 映射接入”，`BridgePromiseErrorMapperTest` 与 `app:testDebugUnitTest` 已通过；后续重点转向 `NavigationBridgeModule` 或 `UserBridgeModule` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步在其余 Bridge 模块继续接入统一错误映射，随后切入 `StorageFacade` |
| 2026-03-17 | Phase 3 | 进展 | `AppError` 已接入 User Bridge 错误映射 | `UserBridgeModule` 的错误处理模式与 Settings 类似，都是 Promise reject 文本散落；直接复用 `BridgePromiseErrorMapper` 可以在不改变协议语义的前提下扩展统一错误模型覆盖面 | `V3-04` 已从“Home/Search + Settings Bridge”推进到“Home/Search + Settings/User Bridge”，`app:testDebugUnitTest` 已持续通过；后续重点转向 `NavigationBridgeModule` 剩余错误分支 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续在 `NavigationBridgeModule` 中接入统一错误映射，进一步逼近 `V3-04` 关闭条件 |
| 2026-03-17 | Phase 3 | 进展 | `AppError` 已接入 Navigation Bridge 主要异常分支 | `NavigationBridgeModule` 是高风险跨边界模块，继续保留散落的 `promise.reject(code, throwable)` 会让首批错误模型统一停在半路；先统一它的主要网络/AI/注册/Effect 异常分支，可以在不改变成功返回结构的前提下显著收敛错误处理 | `V3-04` 已从“Home/Search + Settings/User Bridge”推进到“Home/Search + Settings/User/Navigation Bridge”，`app:testDebugUnitTest` 已通过；后续可开始评估 `V3-04` 是否满足首批关闭阈值，或继续清理少量残余分支 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步判断 `V3-04` 是否已具备 `ready_for_validation` 条件，并把重心切到 `V3-02` 或继续扩大 DataStore 试点 |
| 2026-03-17 | Phase 3 | 进展 | `AiService` 已迁入 `NetworkFacade` 主通路 | `AiService` 在迁移矩阵中属于高风险作者侧能力，但接口面相对集中，适合作为又一个高风险服务层收口样本；保留无参兼容构造后，可以不打断 `NavigationBridgeModule` 现有调用方式 | `V3-01` 已从“Bridge + Home + Search + User”推进到再纳入 `AiService`，`AiServiceTest` 与 `app:testDebugUnitTest` 已通过；后续主网络通路仍剩 `BookService`、`AuthorService` 等更大调用面 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步在 `BookService` / `AuthorService` 中选择一个更小切口继续推进 V3-01 |
| 2026-03-18 | Phase 3 | 进展 | `AuthorService` 的作家状态与注册主路径已迁入 `NetworkFacade` 主通路 | `AuthorService` 调用面比 `BookService` 更集中，且 `getAuthorStatusBlocking/registerAuthorBlocking` 已被 `NavigationBridgeModule` 真实使用，适合作为下一组高风险服务层切口 | `V3-01` 已从“Bridge + Home + Search + User + AI”推进到进一步纳入 `AuthorService` 核心路径，`AuthorServiceTest` 与 `app:testDebugUnitTest` 已通过；后续高风险网络主通路主要剩 `BookService` 与 `AuthorService` 的剩余接口 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步判断是继续拆 `AuthorService` 其余路径，还是转向更大的 `BookService` |
| 2026-03-18 | Phase 3 | 进展 | `BridgeCoroutineScopes` 已开始收口 Bridge 层匿名协程作用域 | `V3-02` 不适合一口气全仓替换，先从 `SettingsBridgeModule` 与 `UserBridgeModule` 这种结构清晰、验证成本低的桥接模块切入，可以先证明统一作用域模型可行 | `V3-02` 已从 `planned` 推进到 `in_progress`，`BridgeCoroutineScopesTest` 与 `app:testDebugUnitTest` 已通过；后续需继续把 `NavigationBridgeModule` 等匿名 `CoroutineScope(Dispatchers.*)` 收口到同一模型 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续扩大 `BridgeCoroutineScopes` 覆盖面，随后再处理 `runBlocking` 场景 |
| 2026-03-18 | Phase 3 | 进展 | `BridgeCoroutineScopes` 已扩展到 Navigation Bridge | `NavigationBridgeModule` 是 Bridge 层匿名协程使用最密集的模块之一，把它收口到共享 scopes 后，`V3-02` 的“统一模型”才不只是局部试点 | `V3-02` 已从“Settings/User Bridge”推进到“Settings/User/Navigation Bridge”三模块覆盖，`app:testDebugUnitTest` 已通过；后续需继续处理 `runBlocking` 与基础设施中的匿名作用域 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步在 `MainApplication` 或 `RequestCoalescingInterceptor` 等高影响点继续推进协程模型统一 |
| 2026-03-18 | Phase 3 | 进展 | `RefactorFeatureFlags` 已进入回滚链路 | `V3-06` 不能只停留在文档层；先把 Bridge 错误映射和 Settings DataStore 试点挂到统一 feature flags 上，可以形成最小可执行的回退入口 | `V3-06` 已从 `planned` 推进到 `in_progress`，`RefactorFeatureFlagsTest` 与 `app:testDebugUnitTest` 已通过；当前网络主通路仍主要依赖兼容层本身保证可回退，后续需继续补更强的网络/存储切换证据 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续增强 kill switch 证据，或在关闭评审时明确哪些回退能力已满足、哪些仍属兼容层保障 |
| 2026-03-18 | Phase 3 | 进展 | `BookService` 阻塞主路径已进一步迁入 `NetworkFacade` 主通路 | `BookService` 属于高频核心阅读链路，书详情、章节列表、正文内容、最新评论和末章摘要这组阻塞接口会直接影响书详情页与阅读准备路径，适合作为 `V3-01` 的连续收口主题；先用新增 JVM 单测锁定请求端点和参数，再将实现切换到 `requestAndParse()` | `V3-01` 已从“`BookService` 榜单/分类主路径”推进到“`BookService` 阻塞主路径整体收口”，`app:testDebugUnitTest --tests com.novel.utils.network.api.front.BookServiceTest` 由红转绿，随后 `app:testDebugUnitTest` 继续通过；后续高风险网络主通路主要剩 `AuthorService` 的剩余生产阻塞路径 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `AuthorService` 剩余阻塞路径，并继续收敛 `V3-06` / `V3-02` |
| 2026-03-18 | Phase 3 | 进展 | `AuthorService` 的发布书籍阻塞路径与旧回调壳已收口 | `AuthorService` 的 `publishBookBlocking()` 仍属于作者侧真实生产能力，且之前是文件内最后一条阻塞写路径旧壳入口；在新增 JVM 单测锁定请求体后，将其切到 `NetworkFacade`，并同步删除已失效的私有回调接口，可以显著缩小 `V3-01` 剩余旧壳面 | `V3-01` 已从“`AuthorService` 核心路径迁移”推进到“`AuthorService` 阻塞生产主路径与旧回调壳清零”，`app:testDebugUnitTest --tests com.novel.utils.network.api.author.AuthorServiceTest` 与 `app:testDebugUnitTest` 均继续通过；后续高风险网络主通路主要剩 `BookService` 的兼容与增量路径 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步回到 `BookService` 兼容/增量路径，或转入 `V3-02` / `V3-06` 的基础设施收口 |
| 2026-03-18 | Phase 3 | 决策 | `V3-01` 达到绿色关闭阈值 | 已为 `BookService` 条件请求增量路径补齐 JVM 单测并迁移到 `NetworkFacade`，同时核验 `NavigationBridgeModule`、`HomeService`、`SearchService`、`BookService`、`UserService`、`AuthorService` 与 `AiService` 文件内旧 `ApiService` 直连为 `0` | `app:testDebugUnitTest --tests com.novel.utils.network.api.front.BookServiceTest` 与 `app:testDebugUnitTest` 持续通过，验证看板中的 `V3-01` 正式切换为 `validated/green` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续阶段关闭重点转向 `V3-02`、`V3-06` 与 `V3-07` |
| 2026-03-18 | Phase 3 | 决策 | `V3-02` 达到绿色关闭阈值 | 已新增 `OnDemandInitializer` 并将 `MainApplication.getOrCreateReactRootView()` 中的 `runBlocking` 改为单次同步初始化入口；同时再次核验 Bridge 模块匿名 `CoroutineScope(Dispatchers.*)` 已统一收口到 `BridgeCoroutineScopes` | `app:testDebugUnitTest --tests com.novel.core.concurrency.OnDemandInitializerTest` 与 `app:testDebugUnitTest` 持续通过；`runBlocking` 仅剩 `RequestCoalescingInterceptor` 这一处 OkHttp 同步 API 边界，已记录为可接受的同步边界而非匿名全局协程风险 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续阶段关闭重点转向 `V3-03`、`V3-04`、`V3-06` 与 `V3-07` |
| 2026-03-18 | Phase 3 | 决策 | `V3-03`、`V3-04`、`V3-06`、`V3-07` 达到绿色关闭阈值 | `StorageFacade` 已成为 Settings/Search/Reader 低风险 key 的统一入口并完成 DataStore 试点；`AppError` 第一批边界已覆盖 Home/Search/Bridge/Settings；Bridge 与存储回退开关已由 `RefactorFeatureFlags` 承接，网络侧则继续由 `NetworkFacade -> LegacyApiServiceAdapter` 保持结构性可逆；Phase 4 进入条件已在阶段文档和计划文档中客观化 | 验证看板中的 `V3-03`、`V3-04`、`V3-06`、`V3-07` 正式切换为 `validated/green`，且 `Phase 3` 文档状态同步切换为 `validated` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 允许控制面板切换到 `Phase 4 planned`，后续按 Phase 4 文档启动边界收口与超大类拆分 |
| 2026-03-18 | 阶段切换 | 决策 | `Phase 3` 正式关闭并切换到 `Phase 4 planned` | `V3-01 ~ V3-07` 均已形成绿色证据，`phase-3-infra-consolidation.md` 与 `phase-3-4-validation-board.md` 已同步更新 | `docs/refactor/README.md` 当前阶段切换为 `Phase 4`，阶段状态为 `planned`，阶段门禁保持严格模式 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续实施必须继续将所有 Phase 4 计划、决策与验证证据落盘到 `docs/refactor/` |
| 2026-03-19 | 文档治理 | 决策 | `Phase 3` 权威总结与 phase-aware staffing 规则已落盘 | 当前主文档体系此前缺少 `Phase 3` 专属权威 closeout、双人交叉评审模板，以及“每个新 phase 先规划 subagent 编制”的固定规则 | 后续 `Phase 4+` 优先读取 `phase-3-closeout-assessment.md` 与 `phase-aware-gpt5.4-subagent-policy.md`；`decision-log.md` 继续保持 append-only，不再作为唯一权威总结入口 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续每个新 phase 文档都必须新增 `协作编制` 区块，并遵循 Leader-only / Lock ID / Rollback ID 规则 |
| 2026-03-19 | Phase 4 | 决策 | `Phase 4` 文档升级为长期自主推进蓝图 | 当前 `Phase 4` 初版文档只具备任务表，不足以支持你不在线时的长期持续推进；因此本轮吸收 `Phase 1/2/3` 的 closeout、门禁、证据与协作经验，补齐双轨执行结构、长期运行协议和波次追踪机制 | `Phase 4` 现在采用 `P4.x + Wave 1~4 + 长期运行协议` 的双轨结构；默认允许在不命中重大决策升级清单时持续自主推进，并要求每轮同步 `phase-4-wave-tracker.md`、`subagent-dispatch-log.md`、`rollback-index.md` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续按 `Wave 1` 启动包边界图、职责切片图和 `BridgeFacade` 外围映射表的准备工作 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A01` 已落下 `NavigationBridgeFacade` 最小兼容壳 | `Phase 4` 需要先证明 `BridgeFacade` 可以在不改变 RN 对外协议的前提下承接最小导航出口；因此先收口登录、设置、返回三条最小路由，作为 `Wave 2` 的第一刀 | `NavigationBridgeFacade.kt` 与 `NavigationBridgeFacadeTest.kt` 已落地，`NavigationBridgeModule` 的三条最小导航出口已改走 facade，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.facade.NavigationBridgeFacadeTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续推进 `W2-A02`，抽离 `NavigationRouteDelegate` 的最小实现 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A02` 已抽离 `NavigationRouteDelegate` 最小实现 | `NavigationBridgeModule` 中存在一批纯 route 跳转方法，适合作为第二刀低风险 delegate 抽离；先收口 `timed_switch`、`help_support`、`privacy_policy`、`history`、`message` 五条纯跳转，可以在不改 route 语义的前提下验证 delegate 拆分可行性 | `NavigationRouteDelegate.kt` 与 `NavigationRouteDelegateTest.kt` 已落地，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationRouteDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过；下一步继续推进 `W2-A03` 查询类 delegate，或并行进入 `W2-A06` 宿主页首开验证 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步推进 `NavigationQueryDelegate` 最小拆分，并准备 `profile/settings` 宿主页验证 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A03` 已抽离 `NavigationQueryDelegate` 最小实现 | 在 `BridgeFacade` 与纯 route delegate 落地后，桥接状态与主题状态这组三条低风险查询出口是下一组最适合作为 delegate 样本的目标；它们不改变 Promise 字段语义，也不依赖高风险网络数据 | `NavigationQueryDelegate.kt` 与 `NavigationQueryDelegateTest.kt` 已落地，`NavigationBridgeModule` 的 `getBridgeStatus`、`getCurrentActualTheme`、`getCurrentNightMode` 已改走 delegate，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationQueryDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步并行评估 `W2-A06` 宿主页首开验证与 `W2-A04` HostDelegate 最小拆分，优先选择证据链更短的一项 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A04` 已抽离 `NavigationHostDelegate` 最小实现 | 在 facade、route、query 三组最小桥接出口收口后，组件注册、路由通知和缓存清理这四条宿主页相关出口是下一组最适合拆出的 host delegate 样本；它们和 `profile-host / RN Host` 风险验证直接相关，但不改变宿主页对外语义 | `NavigationHostDelegate.kt` 与 `NavigationHostDelegateTest.kt` 已落地，`NavigationBridgeModule` 的 `registerComponent`、`notifyRouteChanged`、`clearComponentCache`、`clearAllComponentCache` 已改走 delegate，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationHostDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步优先转入 `W2-A06`，开始 `profile/settings` 宿主页首开验证准备与证据模板收口 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A05` 已抽离 `SelectionMenuDelegate` 最小实现 | `attachSelectionMenu` / `detachSelectionMenu` 这组 Android Selection Menu 逻辑职责单一、协议边界清晰，适合作为下一组低风险 delegate 样本；先收口 action 解析与事件 payload 组装，可以在不改变 `WritePageSelectionMenuAction` 事件语义的前提下减轻 `NavigationBridgeModule` 复杂度 | `SelectionMenuDelegate.kt` 与 `SelectionMenuDelegateTest.kt` 已落地，`NavigationBridgeModule` 中 Selection Menu 的 action 解析和事件 payload 组装已改走 delegate，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.SelectionMenuDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按既定顺序进入 `W2-A06`，开始 `NavigationContentQueryDelegate` 最小拆分 |
| 2026-03-19 | Phase 4 | 进展 | `W2-A06` 已抽离 `NavigationContentQueryDelegate` 最小实现 | 在 facade、route、query、host、selection 这些低风险桥接出口收口之后，6 个读操作的 payload 组装层是下一组最适合抽出的内容查询 delegate；先把映射层搬离 `NavigationBridgeModule`，可以在不改变 Promise 语义和现有 gateway/service 调用路径的前提下继续减重 | `NavigationContentQueryDelegate.kt` 与 `NavigationContentQueryDelegateTest.kt` 已落地，`NavigationBridgeModule` 中 `getReadingHistory`、`getHomeBooksHighPriority`、`getAuthorStatus`、`getAuthorBooks`、`getBookCategories`、`searchBooks` 已改走 delegate 的 payload 组装，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationContentQueryDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `W2-A07`，抽离 `NavigationAuthorDelegate` 最小实现，并并行准备宿主页验证模板 |
| 2026-03-20 | Phase 4 | 进展 | `W2-A07` 已抽离 `NavigationAuthorDelegate` 最小实现 | 在 facade、route、query、host、selection、content-query 这些低风险桥接出口收口后，成为作家/写作入口/作品管理与注册请求构造是下一组最适合抽出的 author delegate 样本；先锁定 route 拼装和注册请求语义，可以继续减轻 `NavigationBridgeModule` 而不改变对外行为 | `NavigationAuthorDelegate.kt` 与 `NavigationAuthorDelegateTest.kt` 已落地，`NavigationBridgeModule` 的 `navigateToBecomeWriterWithFlag`、`navigateToWritePage`、`navigateToBookManage` 与 `registerAuthor` 的请求构造已改走 delegate，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationAuthorDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步转入 `W2-A10` 宿主页验证准备与证据模板，并随后继续 `W2-A08` AI delegate 收口 |
| 2026-03-20 | Phase 4 | 进展 | `W2-A08` 已抽离 `NavigationAiDelegate` 最小实现 | 在 facade、route、query、host、selection、content-query、author 这些低风险桥接出口收口后，AI 4 个出口的成功/失败结果判断是下一组最适合抽出的 AI delegate 样本；先统一 `AiService` 响应到 Promise resolve/reject 的判断逻辑，可以继续减轻 `NavigationBridgeModule` 而不改变错误码前缀与对外语义 | `NavigationAiDelegate.kt` 与 `NavigationAiDelegateTest.kt` 已落地，`NavigationBridgeModule` 的 `aiPolish`、`aiExpand`、`aiCondense`、`aiContinue` 已改走 delegate 的结果判断，`android/gradlew.bat app:testDebugUnitTest --tests "com.novel.rn.bridge.delegate.NavigationAiDelegateTest"` 与 `android/gradlew.bat app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步转入 `W2-A09` `NavigationThemeDelegate` 最小拆分，并在设备可用时开始宿主页首开验证 |
| 2026-03-20 | Phase 4 | 决策 | 设备当前不可用，按静默连续推进协议自动从 Wave 2 切入 Wave 3 | 当前 shell 下执行 `adb devices -l` 返回空设备列表，`W2-A11` 宿主页实证暂时无法开展；但 `V4-05` 还不是最后唯一 blocker，按照既定协议不等待人工确认，先继续推进 `Wave 3` | `phase-4-wave-tracker.md` 当前激活波次已切换到 `Wave 3`，`V4-05` 保持 `in_progress/yellow`，待设备恢复可用后回补正式运行证据 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 继续并行推进 `W3-H01`、`W3-C01` 与 `W3-S01` 的第一批拆分切口 |
| 2026-03-20 | Phase 4 | 进展 | `W2-A10` 宿主页验证准备与证据模板已落地 | 为了让 `profile/settings/author-ai` 宿主页验证不再受“没有标准模板”阻塞，本轮先把三类运行记录模板与 checklist 固定下来；这样真机或 ADB 一旦可用，就能直接执行采证 | `host-risk-run-profile-template.md`、`host-risk-run-settings-template.md`、`host-risk-run-author-ai-template.md` 与 `host-risk-evidence-checklist.md` 已落地，`V4-05` 现在具备完整模板和链路说明，但正式运行证据尚未产出 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续 `W2-A08` AI delegate，若设备可用则穿插执行 `profile/settings` 首开验证 |
| 2026-03-20 | Phase 4 | blocker | `V4-06` 当前仍未达到 repo 级静态债关闭阈值 | `npm run lint` 仍为 `89 errors / 1219 warnings`，与基线持平；`android/gradlew.bat app:detekt` 虽已从 `2260` 收敛到 `2224 weighted issues`，但距离阶段目标 `<= 1921` 仍差 `303` | `touched-files` 口径当前未见新增 detekt 问题，但 repo 级静态债目标仍保持 `yellow`，Phase 4 不能按原关闭口径直接转 `validated` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 若坚持当前阈值，需要额外的大规模 detekt / ESLint 历史债清理；否则需在关闭前重新明确静态债关闭口径 |
| 2026-03-20 | Phase 4 | 决策 | `Wave 3` 的 Gradle 验证改为强制串行执行 | 在 `SearchHistoryStore` 与 `CacheVersionMigrator` 的并行验证过程中，出现了 KSP/Kotlin daemon 共享缓存互踩，触发 `kspDebugKotlin` 内部错误；根因不是业务代码，而是并发执行多个 Gradle 测试任务 | 后续 `Phase 4` 子代理仍可并行做代码与分析，但所有 Gradle 命令必须由 `LeaderAgent` 串行调度执行，避免再次引入非业务 blocker | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 从 `W3-C01` 起，统一按 `定向测试 -> app:testDebugUnitTest` 的串行顺序完成验证与取证 |
| 2026-03-21 | Phase 4 | 进展 | `W2-A11` 宿主页运行证据已补齐并转入待验证 | 当前设备已重新连通 `192.168.8.130:5555`，且已将 `profile / settings / aipage` 三类宿主页的现有截图、XML、logcat 与同轮人工复核结论落盘为正式运行文档 | `V4-05` 已从“设备不可用导致 `in_progress/yellow`”推进到“证据已补齐、`ready_for_validation/green`”；后续 Phase 4 主 blocker 明确收敛为 `V4-06` 的 repo 级静态债阈值 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按既定阈值继续执行大规模 `detekt / ESLint` 历史债清理，先完成 `ESLint error = 0` 再压 `detekt <= 1921` |
| 2026-03-21 | Phase 4 | 决策 | 第二阶段静态债已在当前阈值下达标 | `E1 / E2 / E3` 已将 repo 级 `ESLint error` 从 `89` 清到 `0`；随后 `D1 / NewLineAtEndOfFile` 把 `detekt` 从 `2224` 压到 `2029`，`D2 / WildcardImport` 进一步压到 `1901`。期间 `D2` 首轮显式导入展开曾触发 Kotlin 编译回归，但已通过最小 import 修复恢复 `app:testDebugUnitTest` 通过 | `V4-06` 已从 `in_progress/yellow` 推进到 `ready_for_validation/green`，Phase 4 当前主 blocker 从“静态债未达标”切换为“剩余生产 mock 处置与 closeout 文档闭环” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `V4-04 / V4-07 / V4-08` 的剩余关闭动作，并维持当前静态债阈值不回退 |
| 2026-03-21 | Phase 4 | 决策 | `V4-04` 采用“触达范围收口”关闭口径 | 继续强推所有 RN heavy pages 主数据 mock 在 `Phase 4` 全清，会直接扩大范围并破坏“不改业务功能语义”的阶段约束；用户已确认采用“触达范围收口” | `Phase 4` 只关闭低风险与已触达主路径 mock，页面主数据源仍为 mock 的 RN heavy pages 与 `GetCategoryFiltersUseCase` 默认分类 fallback 统一转入 `Phase 5 entry checklist` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 低风险生产 mock 已按此口径清理完毕，剩余项不得再在 `Phase 4 closeout` 中伪装为已清理 |
| 2026-03-21 | Phase 4 | 决策 | `V4-01 ~ V4-08` 全部达到绿色关闭条件 | `Phase 4` 的边界图、拆分结果矩阵、Bridge 兼容证据、宿主页运行文档、静态债差异、closeout 报告与 `Phase 5 entry checklist` 均已落盘，且 `npm test -- --runInBand`、`android/gradlew.bat app:testDebugUnitTest` 当前通过 | `Phase 4` 正式切换为 `validated`，`Stage 2 = validated`，控制面板切换到 `Phase 5 planned` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续只允许按 `Phase 5 entry checklist` 启动真正模块化准备，不再回滚到 `Phase 4` 范围扩张 |
| 2026-03-21 | Stage 3 Planning | 决策 | 第三阶段定义为 `Stage 3 = Phase 5 + Phase 6` | `Phase 5` 的真正模块化与 `Phase 6` 的性能专项共享同一批稳定模块边界；而 `Phase 7` 的包体积/依赖/构建效率治理更适合在模块图稳定与性能基线刷新后单独展开 | 已新增第三阶段总计划、`Phase 5/6` 阶段文档、`Phase 5-6` 验证看板、第三阶段静态债基线与当前状态文档，控制面板与路线图都可直接引用这组文档 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按 `Stage 3` 文档体系启动 `Phase 5 planned`，先落模块图与依赖规则，再进入真正的模块拆分 |
| 2026-03-21 | Phase 5 | 进展 | 打开书籍运行时 blocker 已修复并纳入持续回归门禁 | 阅读器打开链路此前会因 `BookService` 在章节更新时间为空时参与比较而触发“请求错误”；该问题已通过空值兼容处理修复，并补入 `BookServiceTest` 专项回归用例 | `Phase 5` 的模块化工作不再建立在未解运行时 blocker 上，后续所有新切口都必须先保持该回归用例为绿 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 继续以 `BookService` 回归测试作为 Phase 5 首个固定门禁 |
| 2026-03-21 | Phase 5 | 进展 | `core-storage` 与契约优先版 `core-network` 已落地，Phase 5 进入真实实施态 | `core-storage` 已完成首个稳定 `core/*` 模块抽离，`core-network` 已完成首批契约层抽离并通过模块级验证；控制面板与阶段文档若仍保持 `planned` 会与真实仓库状态漂移 | `Phase 5` 从“文档准备”切换到“真实模块化实施”，后续固定顺序调整为 `doc/state sync -> build conventions -> core-common -> deepen core-network -> core-bridge-contract -> feature-welfare -> feature-search -> feature-home -> feature-rn-host -> validation/closeout` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步先完成 Stage 3 文档状态纠偏，再继续 `core-common` 切口 |
| 2026-03-21 | Phase 5 | 进展 | 共享构建约定与 `core-common` 第一刀已落地 | `core/*` 模块已开始出现重复 library 配置，且 `BaseMviViewModel / BaseUseCase / AppError / StableFlow` 等共享基础仍压在 `app`；本轮先新增 `android-library-common.gradle`，再将第一批 app-independent core 基础迁入 `android/core-common` | `Phase 5` 获得了首个共享基础模块与可追溯的当前模块图证据，后续 `core-network` 深化与 `core-bridge-contract` 将建立在这个新边界之上 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续推进 `core-bridge-contract` 最小切口，并同步更新 rollback / dispatch 记录 |
| 2026-03-21 | Phase 5 | 决策 | 暂停当前 `core-network` 深化切口，先转入 `core-bridge-contract` | 直接搬迁共享网络原语时，默认 `app` 编译链在 `kaptGenerateStubsDebugKotlin` 上出现不可接受的不稳定；为避免把主线卡死在高噪声集成问题上，本轮不继续扩大该切口 | `core-network` 仍保留“契约优先”已验证成果，后续将以更小原子主题继续深化；`Phase 5` 当前执行顺序临时调整为先关闭 `core-bridge-contract` 再进入 `feature-welfare` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续重新设计 `core-network` 深化的更小切口，并保持 Gradle 串行验证 |
| 2026-03-21 | Phase 5 | 进展 | `core-bridge-contract` 第一刀已落地 | 已将 `NavigationHostDelegate`、`NavigationRouteDelegate`、`SelectionMenuDelegate` 及其单测迁入 `android/core-bridge-contract`，并保持 app 侧调用与桥接语义不变 | `V5-02` 现已具备四个首批 `core/*` 模块全部落地的证据，`core-bridge-contract:testDebugUnitTest` 与 `app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `feature-welfare` 首轮最小切口，并补齐 rollback / dispatch / 模块图状态同步 |
| 2026-03-21 | Phase 5 | 进展 | `feature-welfare` 首轮最小切口已落地 | welfare 现阶段仍受 `WelfarePage` 宿主 wrapper、主题、对话框、`StateAdapter` 等 blocker 约束；因此本轮先按“app 做宿主，feature 承载纯组件”的保守策略，将 `EnhancedErrorComponent / SkeletonLoadingComponent` 迁入 `android/feature-welfare` | `V5-03` 已从 `planned` 推进到 `in_progress`，`feature-welfare:compileDebugKotlin` 与 `app:testDebugUnitTest` 当前通过，后续可继续扩大 welfare feature 内部边界 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步评估 welfare 下一批可迁组件，或切入 `feature-search / feature-home` 的首轮最小模块 |
| 2026-03-21 | Phase 5 | 进展 | `feature-welfare` 已扩展到内部工具层 | 在首轮纯组件迁移稳定后，`WelfarePerformanceMonitor / WebViewPreloadManager / WelfareAccessibilityHelper` 这组三类低耦合内部工具已具备脱离 `app` 的条件；它们不依赖 `WelfarePage` 宿主 wrapper、主题管理或 URL 配置 | `feature-welfare` 现已不只承载 UI 组件，还开始承载内部 feature 基础设施；`feature-welfare:compileDebugKotlin` 与 `app:testDebugUnitTest` 继续通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续扩大 welfare feature 内部边界，并并行推进 `feature-search / feature-home` 首轮切口 |
| 2026-03-21 | Phase 5 | 决策 | `feature-home` 与 `feature-search` 首轮切口按联动原子主题提交 | 当前 `HomePerformanceOptimizer` 与 `SearchPreferenceStorage` 各自都属于低风险 helper/store 切口，但两者都会立即被 app 现有代码引用；若强行拆成两个完全独立提交，中间态会出现 build graph 引用缺口 | 本轮允许以“linked first feature slices”的方式合并提交，并在 rollback / dispatch 中明确记录该联动关系；后续更深层切口仍应尽量保持单主题原子粒度 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续扩大 home/search 的低耦合边界，直到可以恢复完全独立的 feature 提交节奏 |
| 2026-03-21 | Phase 5 | 进展 | `feature-home` 与 `feature-search` 首轮最小切口已落地 | `feature-home` 先迁出 `HomePerformanceOptimizer`，`feature-search` 先迁出 `SearchPreferenceStorage`；它们都属于低耦合切口，且在当前组合入口下已通过模块级验证与 `app:testDebugUnitTest` | `V5-03` 继续前推，首批 feature 模块不再只有 welfare 一条线；后续可继续扩大 `feature-home / feature-search` 的 helper/store/coordinator 切口 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `feature-rn-host` 首轮切口，或继续扩大 welfare/search/home 内部边界 |
| 2026-03-21 | Phase 5 | 进展 | `feature-rn-host` 首轮最小切口已落地 | 当前 RN Host 真正的 React/Application 宿主逻辑仍需留在 `app`；因此本轮先按最保守策略，将仅依赖 `core-storage` 的 `SettingsPreferenceStorage` 与其单测迁入 `android/feature-rn-host` | `V5-03` 现已具备四条首批 feature 线全部起步的事实基础，`feature-rn-host:testDebugUnitTest` 与 `app:testDebugUnitTest` 当前通过；后续可继续扩大 host-support 层，但仍不得碰 `MainApplication / NavigationPackage / ReactNativePage` 等宿主根逻辑 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `V5-04 / V5-05 / V5-06` 收尾主线，并择机继续扩大各 feature 内部边界 |
| 2026-03-21 | 阶段切换 | 决策 | `Phase 5` 正式关闭为 `validated`，当前阶段切换为 `Phase 6 planned` | `V5-01 ~ V5-06` 已全部具备绿色验证证据，且 `README`、阶段文档、模块图快照、兼容证据与命令矩阵已同步闭环 | Phase 5 退出条件已满足；Stage 3 后续重心从模块化转向性能 baseline 与专项治理 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步按 `phase-6-performance-governance.md` 启动 Phase 6 |
| 2026-03-21 | 阶段切换 | 决策 | `Phase 5` 权威 closeout 已落盘，`Phase 6` 从已验证模块图正式启动 | 当前仓库虽已证明 `Phase 5 = validated`，但此前缺少专属 closeout 报告和首份 Phase 6 baseline evidence 文档；本轮已补齐二者，并把 `Phase 6` 状态从 `planned` 切到 `in_progress` | Stage 3 现在从“模块化阶段”进入“性能 baseline 启动阶段”；`V6-01` 进入 `in_progress`，其余 `V6-*` 保持 planned | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步优先稳定 macrobenchmark connected runs，并继续补性能证据 |
| 2026-03-21 | Phase 6 | 决策 | 默认 green benchmark 套件切换为 no-compilation，compiled-mode 路径降级为探针 | `DN2101` 上 `cmd package compile` 对 `com.novel` 与 `com.android.settings` 均复现 `Error: Failed to cpmpile !`；若继续把 compiled-mode 用例留在默认 suite，会把环境噪声误判成仓库回归 | `ExampleStartupBenchmark` 与 `ScrollPerformanceBenchmark` 现在只承载稳定的 no-compilation baseline；`StartupCompilationProbeBenchmark`、`ScrollCompilationProbeBenchmark` 与 `BaselineProfileGenerator` 保留为 blocker probe，不再阻塞日常性能基线积累 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续在第二设备上重试 compiled-mode startup/profile，再决定是否升级为硬门禁 |
| 2026-03-21 | 阶段切换 | 决策 | `Phase 6` 正式关闭为 `validated`，`Stage 3` 切换为 `validated`，当前阶段切换到 `Phase 7 planned` | `V6-01 ~ V6-06` 已全部形成正式证据与 closeout；startup / scroll baseline 已绿，baseline profile blocker 已以 accepted environment blocker 方式留痕，Search / Reader / Welfare-WebView / RN Host / Bridge 均有专项文档 | `Stage 3` 不再保持 open/current-state 口径；后续主线固定转向 `Phase 7` 的 size / dependency / build governance，不再回流到 `Phase 6` 的 baseline 收集主线 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 进入 `Phase 7`，建立包体积、依赖与构建效率 baseline |
| 2026-03-21 | Phase 6 | 决策 | `Phase 6` 达标关闭，但不等于完成全部优化；剩余优化机会转为显式 backlog | 当前 `Phase 6` 的完成重点是基线、证据、预算和 blocker 固化，而不是把所有启动、Reader、WebView、RN Host、缓存与数据库优化全部做完；若不显式留痕，后续容易把“已达标”误读成“无优化空间” | 新增 `blueprint-v2-phase-3-6-gap-analysis.md` 与 `phase-6-optimization-opportunity-catalog.md` 作为正式补充文档；后续若继续推进性能收益优化，应从这些文档中选择条目立项，而不是重新定义 `Phase 6` 的关闭结论 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续按 backlog 继续推进真实优化项，并在未来专项中补充新的证据闭环 |
| 2026-03-22 | Phase 3 | 进展 | `bad7f5f` `补齐网络请求追踪头` | 原蓝图中的 request / trace id 能力此前只停留在文档差异，缺少已提交代码事实支撑；本轮先用最小侵入方式在 `OkHttp` 链路补入统一 header 注入 | `NetworkModule` 已默认接入 `RequestIdInterceptor`，请求链路开始统一补齐 `X-Request-Id / X-Trace-Id`；`Phase 3` 的该项结论应从“未实现”修正为“部分实现”，但 observability 闭环仍待后续治理 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续在审计文档中明确写为“header 注入已落地，仓库级闭环未完成” |
| 2026-03-22 | Phase 6 | 进展 | `c9f1f8e` `收敛阅读器初始化与分页刷新` | Reader init 与设置变更路径仍有重复触发风险，若继续放任，会让 `Phase 6` 文档长期停留在“只做了测量”的过期口径 | `ReaderStartupCoordinator` 与 `ReaderSettingsRefreshCoordinator` 已落地，Reader 初始化去重、设置触发分页刷新收敛生效；`Phase 6` 对 Reader 的结论应更新为“已有局部优化 + 动作级治理未完成” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续补 Reader flip / settings 动作级直接样本与更细粒度压测 |
| 2026-03-22 | Phase 6 | 进展 | `666cc72` `收敛福利页初始化路径` | Welfare 页此前存在 bootstrap 时序过宽的问题，容易让性能文档继续沿用“重复初始化嫌疑未处理”的旧表述 | `WelfarePageBootstrapCoordinator` 已将 preload manager、performance monitor 与 `InitializePage` 的首次启动收敛到统一计划中；Welfare / WebView 文档应从“初始化嫌疑”切换为“已完成首轮收敛，仍缺更深层专项” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续补 Welfare / WebView 更深层 benchmark 与重复上报治理 |
| 2026-03-22 | Phase 6 | 进展 | `f679aa6` `延后搜索分类筛选加载` | 搜索结果页初始化阶段同步承担 `loadCategoryFilters()` 成本，会放大首开负担并让 `Phase 6` 文档对 Search 的描述失真 | `SearchCategoryFilterLoadCoordinator` 已把分类筛选加载改为按搜索启动或筛选面板打开时触发；Search 文档应更新为“已完成首轮首开减负，但 benchmark 化与热点治理未完成” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续补搜索结果页 benchmark、分页与筛选热点治理 |
| 2026-03-22 | Phase 6 | 进展 | `bda823f` `补发RN主题同步` | `ThemeChanged` 之前在 RN context 未就绪时会直接丢失，导致 RN Host 文档长期停留在“主题同步有已知缺口”的旧事实 | `ReactNativeThemeSyncCoordinator` 已在 RN context 就绪后补发主题同步；RN Host / Bridge 文档应改写为“主题补发已落地，剩余问题在生命周期与性能规范” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续治理 `ReactRootView` 生命周期、批量调用与线程切换策略 |
| 2026-03-22 | Phase 6 | 进展 | `28ec92d` `延后非关键启动初始化` | 启动专项若只停留在基线采集，会与原蓝图“真实收益优化”目标产生持续偏差；需要先落一个低风险、可验证的启动减负切口 | `StartupDeferredInitializationCoordinator` 已将 `RetrofitClient`、`SettingsUtils` 等非关键初始化延后到首帧后；启动文档不应再继续写成“只有测量，没有优化” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续拆分 warm / cold path 并复核首帧后任务收益 |
| 2026-03-22 | Stage 3 follow-up | 进展 | `955d9d0` `恢复首页分类栏首页入口` | `android/app` 内首页本地 special filter 的展示语义漂移为 `推荐`，会影响 app 层持续收口后的用户可见一致性 | 首页顶部分类栏首项已统一恢复为 `首页`，同时兼容旧值 `推荐`；这是 closeout 后继续收口的 app 层兼容修正，不改变 `Phase 5/6` 状态 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续保持 app 层本地分类语义、状态与测试口径一致 |
| 2026-03-22 | Phase 6 | 进展 | `bb75095` `延后RN预热到首帧后` | `ComposeMainActivity` 在首屏创建期过早触发 RN 预热，会和冷启动预算产生竞争；若文档仍写“无条件预热”，会继续失真 | `ReactNativePrewarmCoordinator` 已把 RN 预热调整为首帧后触发；启动 / RN Host 文档应更新为“已完成首轮时机优化，后续仍需核算 warm / cold path 收益” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续在第二设备与更细粒度 trace 上复核 RN 预热收益 |
| 2026-03-22 | Phase 6 | 进展 | `463323c` `接通福利页WebView性能埋点` | `WelfarePerformanceMonitor` 中 `FCP / TTI` 能力此前存在但未真正接入，导致文档把该项长期列为“仅有代码入口” | `WelfareWebPerformanceCoordinator` 已在 `WebViewComponent` 中接通 `recordFirstContentfulPaint()` 与 `recordTimeToInteractive()`；Welfare / WebView 文档应更新为“已接线，仍缺更深层专项与收益复盘” | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续补 Welfare / WebView 的宏基准、复开路径与重复上报治理 |
| 2026-03-22 | Phase 6 | 进展 | 收敛阅读器恢复提示与动作探针 | Reader 恢复提示此前仍依赖固定 `delay(1000)` / `delay(3000)`，同时缺少 `flip / settings` 的动作级性能留痕，导致后续专项只能停留在 init 基线层面 | `ReaderRestoreHintCoordinator` 已将恢复提示改为“恢复入口 + 初始化成功 + 首个页面数据就绪”后显示；`ReaderViewModel` 也已补上 `init / flip / settings_update` 的轻量性能 probe，为后续动作级预算与样本归档提供统一入口 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续基于新增 probe 补 Reader flip / settings 的正式 evidence doc 与预算值 |
| 2026-03-22 | Phase 6 | 进展 | 细化搜索结果页性能探针与重试策略 | Search 结果页此前只有统一搜索重试链，无法区分首次进入、分类切换、筛选应用与 `load more` 的行为来源，也会让分页失败语义过重 | `SearchResultViewModel` 已为 `INITIAL_ENTRY / CATEGORY_SWITCH / FILTER_APPLY / LOAD_MORE` 补上统一性能 trace，并将 `load more` 调整为失败即回滚页码、不再执行多轮自动重试；分类筛选加载失败继续不阻塞搜索主链 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续基于新增 trace 补搜索结果页专项 benchmark 与热点治理证据 |
| 2026-03-22 | Phase 6 | 进展 | 补齐RN宿主冷热路径追踪 | 现有 RN Host 证据可以证明兼容不回归，但还无法稳定区分 RN context 已存在、首次创建、`ReactRootView` 复用与首次创建这些冷热路径 | `ComposeMainActivity` 与 `MainApplication` 已通过 `ReactNativeHostPathTraceCoordinator` 补上 RN context / `ReactRootView` 的冷热路径 trace；当前不会改变预热阈值与 RootView 缓存策略，只增强可追踪性 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续基于新增 trace 分析 warm/cold path 收益，并决定是否需要更细粒度 host 生命周期优化 |
| 2026-03-22 | Phase 6 | 进展 | 加固福利页WebView性能上报去重 | Welfare 页此前虽然已经接通 `FCP / TTI` 埋点，但 `pageLoadComplete` 与其他指标仍可能在同一次加载中重复记录，影响后续取证可信度 | `WelfarePage` 与 `WebViewComponent` 现已共享 `WelfareWebPerformanceCoordinator`，并为同一轮加载补上 `FCP / TTI / pageLoadComplete` 的 once-only 保护；当前不改变 URL 白名单、导航语义或外链策略 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续补 Welfare / WebView 的宏基准、复开路径与预加载收益分析 |
| 2026-03-22 | Phase 3 | 进展 | 扩展DataStore试点到设置与用户态 | `SettingsDataStorePilot` 此前只覆盖 `follow_system_theme / auto_night_mode` 两个低风险布尔值，无法真实反映当前写链收口进度 | `SettingsDataStorePilot` 已扩展为设置域 + 用户态的统一 snapshot / migrate / mirror 试点；`SettingsUtils`、Reader 设置写链与登录态写链已开始接入 mirror，但仓库级主读路径仍未切换到 DataStore | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续补 request/trace 观测闭环，并决定是否需要继续扩大 DataStore pilot 的真实读路径 |
| 2026-03-22 | Phase 3 | 进展 | 补齐主网络壳与桥接网关请求追踪日志 | 当前 request / trace id 已能在 `OkHttp` 层生成，但如果缺少边界级结构化日志，主 legacy 网络壳与 bridge gateway 仍然很难被真正追踪 | `RequestIdInterceptor` 已抽出可复用补头 helper，并把结构化 trace 日志补到 `ApiService / RetrofitClient / NavigationBridgeNetworkGateway`；当前已实现“legacy 网络壳 + bridge gateway 可追踪”，但仍不是全仓 observability 终局 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 后续继续把 trace id 带入更多高风险链路与 evidence/doc 入口，逐步收拢到统一 observability 方案 |
| 2026-03-22 | Phase 6 | 进展 | 补齐数据库索引与 FTS4 治理报告 | `Phase 6` 对数据库专项此前只有蓝图目标，没有稳定的治理入口和正式报告，导致 DB 优化项长期停留在“纯待办” | `DatabaseGovernanceReportGenerator` 已落地，并新增数据库索引与 FTS4 治理报告文档；当前已固定索引、FTS4 表/触发器与关键查询计划探针，但索引收益与 `FTS4` 复盘仍待后续证据闭环 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续补缓存治理报告，并在后续专项中归档真实 `EXPLAIN QUERY PLAN` 结果 |
| 2026-03-22 | Phase 6 | 进展 | 补齐缓存清理治理报告 | 缓存专项此前只有 `NetworkCacheManager` 的既有清理逻辑和零散统计，无法稳定回答“当前 cache 多大、cleanup 发生了几次、前后条目量变化多少” | `CacheGovernanceReportGenerator` 已落地，`CleanupStats` 也已扩展为“次数 + 原因 + 耗时 + 前后条目量”；当前缓存专项已经具备正式治理报告入口，但 IO / 内存 / 电量收益仍待后续证据闭环 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步执行 Phase 3-6 文档收尾同步，把 DB/cache 当前状态统一追平到 closeout 体系 |
| 2026-03-23 | 阶段切换 | 决策 | `Phase 5` 与 `Stage 3` 从历史 closeout checkpoint 重新切回 `in_progress` | `2026-03-21` 的首轮 closeout 已证明“最小切口版本”可关闭，但当前模块图仍明显偏浅，`core-ui / core-bridge / feature-book / feature-login / feature-reader` 尚未落地，且首页首开自动加载与 Community 收口仍未完成；继续维持 `Phase 7 planned` 会与真实代码工作面脱节 | 控制面板、Stage 3 计划、Phase 5 文档、验证看板与历史 closeout 文档统一切回“保留 checkpoint、当前继续推进”的口径；`Phase 6` 继续保持 `validated`，`Phase 7` 继续保持 `planned` | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 reopened `Phase 5` 主线，先深化模块图并修复首页首开与 Community 收口 |
| 2026-03-23 | Phase 5 | 进展 | 新增 `core-ui` 并迁移首批通用 UI 基础件 | `core-ui` 属于原蓝图明确要求但此前未落地的核心模块；若继续停留在 `app` 中，后续 feature 深化会持续反向依赖主题、尺寸与基础 Compose 工具 | `android/core-ui` 已落地并接管 `ThemeManager / NovelTheme / flydp / debounceClickable / StableCallbacks / NovelText / NovelTextField / NovelButton / NovelDivider` 及字体资源；`core-ui:testDebugUnitTest` 与 `app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续深化 `core-bridge / core-network`，并在后续文档收尾时补齐 rollback 记录 |
| 2026-03-23 | Phase 5 | 进展 | 深化 `core-network` 与 `core-bridge` 共享边界 | `core-network` 若继续停留在契约壳、`core-bridge` 若继续把共享契约和网关留在 `app`，后续 feature 深化会持续受制于宿主耦合 | `android/core-bridge` 已落地并接管 `BridgeIntent / BridgeState / BridgeEffect / BridgeReducer / BridgePromiseErrorMapper / DefaultNavigationBridgeFacade / NavigationBridgeNetworkGateway`；`android/core-network` 已接管 `RequestIdInterceptor / NetworkTraceLogHelper / ImmutableListTypeAdapter`；`:core-network:testDebugUnitTest`、`:core-bridge:testDebugUnitTest` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `feature-home` 深化与首页首开自动加载修复主线，并在后续文档收尾时补齐 rollback 记录 |
| 2026-03-23 | Phase 5 | 进展 | 深化 `feature-home` 并修复首页首开自动加载 | 首页首开链路此前会把 `CACHE_FIRST` 的空结果误判为成功，导致用户进入首页后需要手动刷新；同时 `feature-home` 若仍只有一个性能 helper，阶段文档会继续失真 | `feature-home` 已新增 `CategoryInfo / HomeCategoryFilterSupport / HomeRestoreCoordinator`，`HomeCompositeUseCase` 已补齐首页推荐空态的 `NETWORK_ONLY` 单次补拉，`HomeViewModel` 已用 restore 协调器替换空壳状态检查；`:feature-home:testDebugUnitTest` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续深化 `feature-search / feature-welfare / feature-rn-host`，并在后续文档收尾时补齐 rollback 记录 |
| 2026-03-23 | Phase 5 | 进展 | 深化 `feature-search / feature-welfare / feature-rn-host` 内部协调层 | 这三条 feature 线此前仍偏“薄壳模块”，如果不把状态契约与协调器迁进去，后续完整模块化会继续被 app 宿主层反向牵制 | `feature-search` 已接管 `SearchResultMvi / SearchTriggerSource / SearchParams / SearchCategoryFilterLoadCoordinator / SearchPerformanceTraceCoordinator / SearchRetryPolicyCoordinator`；`feature-welfare` 已接管 `WelfarePageBootstrapCoordinator / WelfareWebPerformanceCoordinator`；`feature-rn-host` 已接管 `ReactNativeThemeSyncCoordinator / ReactNativeHostPathTraceCoordinator`；三条模块测试与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 `feature-book / feature-login / feature-reader` 主线，并在后续文档收尾时补齐 rollback 记录 |
| 2026-03-23 | Phase 5 | 进展 | 新增 `feature-book / feature-login / feature-reader` 并迁入首批纯逻辑层 | 若这三条线继续完全留在 `app`，`Phase 5` 对蓝图中 `feature-login / feature-reader` 的结论会持续失真，Book 线也无法形成真正的 feature 模块入口 | `feature-book` 已接管 `BookDetailMvi / formatWordCount`；`feature-login` 已接管 `LoginMvi / LoginReducer / LoginStateUpdater`；`feature-reader` 已接管 `ReaderStartupCoordinator / ReaderRestoreHintCoordinator / ReaderPerformanceTraceCoordinator`；`:feature-book:testDebugUnitTest`、`:feature-login:testDebugUnitTest`、`:feature-reader:testDebugUnitTest` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 Community 收口，并在后续文档收尾时补齐 rollback 记录 |
| 2026-03-23 | Phase 5 | 进展 | 打通 Community 现有跳转与分享 | Community 页此前仍保留多条死 TODO，导致首页模块化虽已深入，但书架社区页仍缺最基本的桥接收口；若继续拖延，会让 Stage 3 文档对 RN 页现状失真 | Community 当前已补齐评论详情跳转、搜索/通知/发布桥接与原生分享，`handleMore` 也已改成显式受支持菜单；`handleUserPress / handleSubscribe` 已改为显式 deferred 提示；对应 Jest 与 NavigationBridge contract 测试通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步只剩验证矩阵、closeout 与 rollback 文档收尾 |
| 2026-03-24 | Phase 5 | 进展 | 收敛首页首开榜单与推荐流的空缓存补拉 | `3265a29` 已修复首页推荐流在 `CACHE_FIRST` 空结果下的首开补拉，但榜单切换与首页筛选分支仍可能把空缓存误判为成功，导致首页首开后需要等待页面切换或生命周期变更才展示数据 | `HomeCompositeUseCase` 已统一为首页推荐流、首页筛选分支和榜单加载补齐 `CACHE_FIRST -> NETWORK_ONLY` 空结果补拉，`HomeCompositeUseCaseTest` 已新增对应回归用例；`android/gradlew.bat :app:testDebugUnitTest --tests "com.novel.page.home.usecase.HomeCompositeUseCaseTest"` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步进入 Wave A 文档追平，并继续深化 `core-network / core-bridge` |
| 2026-03-24 | Phase 5 | 进展 | 将 `LegacyApiServiceAdapter` 共享适配层迁入 `core-network` | `LegacyApiServiceAdapter` 逻辑上已经属于 `com.novel.core.network`，但物理上仍留在 `app`；如果继续维持这种状态，`core-network` 仍会停留在“契约壳 + 少量工具件”的浅层形态 | `LegacyApiExecutor / LegacyApiServiceAdapter` 与对应 JVM 测试已迁入 `android/core-network`，`DefaultLegacyApiExecutor` 当前以宿主壳形式保留在 `app` 的独立子包；`:core-network:testDebugUnitTest --tests "com.novel.core.network.LegacyApiServiceAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续选择下一组 `core-network / core-bridge` 稳定共享件做原子切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `StateAdapter` 迁入 `core-common` | `StateAdapter` 当前同时被 Home/Bridge 等多条线复用，却仍留在 `app` 并依赖 `BuildConfig / TimberLogger`；如果不先把它收进共享基础层，后续 `core-bridge` 深化会持续被反向卡住 | `StateAdapter / StateComparator / StateUpdateListener` 已迁入 `android/core-common`，内部日志改为 `CoreLogger`，并补齐 `lifecycle-runtime-compose` 与对应 JVM 测试；`:core-common:testDebugUnitTest --tests "com.novel.core.adapter.StateAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步重新评估 `core-bridge` 共享状态件切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `BridgeStateAdapter` 迁入 `core-bridge` | `BridgeStateAdapter` 虽属于桥接共享状态层，但此前因 `StateAdapter` 仍留在 `app` 而无法安全迁移；在共享基础层障碍清除后，这一层继续留在 `app` 已不再合理 | `BridgeStateAdapter / BridgeScreenState / BridgeStateListener` 与对应 JVM 测试已迁入 `android/core-bridge`；`:core-bridge:testDebugUnitTest --tests "com.novel.rn.bridge.BridgeStateAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续挑选下一组 `core-bridge` 稳定共享件做原子切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `RefactorFeatureFlags` 接口层迁入 `core-common` | `RefactorFeatureFlags` 当前被 bridge/settings/login/reader 多处复用，但如果接口与默认值模型继续留在 `app`，这些模块对共享开关的依赖仍会被宿主实现耦合污染 | `RefactorFeatureFlags / RefactorFeatureFlagDefaults` 已迁入 `android/core-common`，`app` 当前仅保留 `NovelUserDefaultsBackedRefactorFeatureFlags` 实现；`android/gradlew.bat :app:testDebugUnitTest --tests "com.novel.core.config.RefactorFeatureFlagsTest"` 与 `android/gradlew.bat :app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步评估 `DispatcherProvider` 或 `NovelUserDefaultsBackedRefactorFeatureFlags` 的继续拆分路径 |
| 2026-03-24 | Phase 5 | 进展 | 将 `NovelUserDefaultsBackedRefactorFeatureFlags` 迁入 `core-storage` | `RefactorFeatureFlags` 只迁接口层仍不够，若具体实现继续留在 `app`，bridge/settings/login/reader 对共享开关的依赖仍会被宿主实现污染 | `NovelUserDefaultsBackedRefactorFeatureFlags` 与对应测试已迁入 `android/core-storage`，`app` 侧注入和 `SettingsUtils` 的直接构造路径都已追平到共享实现；`:core-storage:clean :core-storage:testDebugUnitTest --tests "com.novel.core.config.RefactorFeatureFlagsTest" --no-daemon` 与 `:app:testDebugUnitTest --no-daemon` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `OptimizedDispatcherProvider` 或 feature 线的下一组实现层切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `DispatcherProvider` 接口层迁入 `core-common` | `DispatcherProvider` 同时被 Reader 与 Bridge 使用，如果接口层继续留在 Reader 包路径下，后续 `core-bridge` 深化会持续被阅读器宿主实现反向牵制 | `DispatcherProvider / DefaultDispatcherProvider` 已迁入 `android/core-common`，`app` 当前保留 `OptimizedDispatcherProvider` 与兼容 typealias 入口；`android/gradlew.bat :app:testDebugUnitTest --tests "com.novel.rn.bridge.BridgeCoroutineScopesTest"` 与 `android/gradlew.bat :app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续推进 `BridgeCoroutineScopes` 等桥接共享调度层切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `BridgeCoroutineScopes` 迁入 `core-bridge` | `BridgeCoroutineScopes` 虽然只负责桥接侧共享协程作用域，但此前被 `DispatcherProvider` 的 Reader 包路径绑在 `app`；在接口层迁入 `core-common` 后，这层继续留在宿主模块已无必要 | `BridgeCoroutineScopes` 与对应 JVM 测试已迁入 `android/core-bridge`，桥接侧共享协程作用域不再依赖 `app` 宿主目录；`:core-bridge:testDebugUnitTest --tests "com.novel.rn.bridge.BridgeCoroutineScopesTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续挑选 bridge 宿主 glue 与状态层剩余共享件 |
| 2026-03-24 | Phase 5 | 进展 | 将 `LoginStateAdapter` 迁入 `feature-login` | `LoginStateAdapter` 当前只依赖登录 feature 自身 MVI 契约和共享 `StateAdapter`，继续留在 `app` 会让登录状态层边界和模块图口径失真 | `LoginStateAdapter` 与对应 JVM 测试已迁入 `android/feature-login`；`:feature-login:testDebugUnitTest --tests "com.novel.page.login.viewmodel.LoginStateAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `feature-book / feature-reader` 的状态适配层切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `StateHolderImpl` 迁入 `core-common` | `StateHolderImpl` 当前虽然只被书籍详情状态适配层使用，但本质上是通用状态容器；如果继续留在 `app` 组件目录，后续 `feature-book` 深化会持续被宿主 UI helper 反向牵制 | `StateHolderImpl` 与对应 JVM 测试已迁入 `android/core-common`，`BookDetailStateAdapter / BookDetailViewModel` 已切换到共享层导入；`:core-common:testDebugUnitTest --tests "com.novel.core.ui.StateHolderImplTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续推进 `feature-book` 的状态适配层迁移 |
| 2026-03-24 | Phase 5 | 进展 | 将 `BookDetailStateAdapter` 迁入 `feature-book` | 在 `StateHolderImpl` 迁入共享层后，`BookDetailStateAdapter` 已不再被 `app` 组件目录绑住；如果继续保留在宿主模块，书籍详情 feature 的状态层边界仍会失真 | `BookDetailStateAdapter` 与对应 JVM 测试已迁入 `android/feature-book`，`BookDetailViewModel` 继续通过共享层 `StateHolderImpl` 访问 UI 状态；`:feature-book:testDebugUnitTest --tests "com.novel.page.book.viewmodel.BookDetailStateAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `feature-reader` 的状态层或 `feature-home` 的协调层切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `SearchResultStateAdapter` 迁入 `feature-search` | `SearchResultStateAdapter` 直接依赖已经落在 `feature-search` 的 `SearchResultState` 契约，继续留在 `app` 会让搜索结果页状态层边界和模块图口径失真 | `SearchResultStateAdapter` 与对应 JVM 测试已迁入 `android/feature-search`；`:feature-search:testDebugUnitTest --tests "com.novel.page.search.viewmodel.SearchResultStateAdapterTest"` 与 `:app:testDebugUnitTest` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `feature-home` 或 `feature-reader` 的下一个低风险切口 |
| 2026-03-24 | Phase 5 | 进展 | 将搜索首页 MVI 契约与状态层继续迁入 `feature-search` | `SearchMvi / SearchReducer / SearchStateAdapter` 之前仍留在 `app`，即便结果页状态层已经进模块，搜索首页仍然会把主契约层留在宿主模块，导致 feature 边界继续失真 | `SearchMvi / SearchReducer / SearchStateAdapter` 以及 `SearchRankingItem` 数据模型已迁入 `android/feature-search`；`:feature-search:testDebugUnitTest --tests "com.novel.page.search.viewmodel.SearchStateAdapterTest"` 与 `:app:testDebugUnitTest --no-daemon` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续回到 `feature-home` 或 `feature-reader` 继续推进内部契约层 |
| 2026-03-24 | Phase 5 | 进展 | 将首页专属 Room 实体迁入 `feature-home` | `HomeBookEntity / HomeBannerEntity / HomeCategoryEntity` 当前只服务首页链路和其本地缓存，如果继续留在 `app`，首页契约层与数据模型边界会持续被宿主模块绑住 | 首页专属 Room 实体已迁入 `android/feature-home`，`HomeDao / HomeRepository / NovelDatabase / HomeState*` 等宿主侧使用点均已切到 feature 依赖；`:feature-home:testDebugUnitTest --tests "com.novel.page.home.dao.HomeEntityTest"` 与 `:app:testDebugUnitTest --no-daemon` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `HomeMvi / HomeStateAdapter / HomeInitialLoadCoordinator` 的迁移路径 |
| 2026-03-24 | Phase 5 | 进展 | 将 `PageFlipEffect / ReaderServiceConfig` 迁入 `feature-reader` | 这两类阅读器共享类型虽然仍被宿主和服务层复用，但它们本质上属于 reader feature 内部配置与交互语义；继续留在 `app` 会让阅读器模块边界停在过浅层级 | `PageFlipEffect` 与 `ReaderServiceConfig` 已迁入 `android/feature-reader`，Reader 与宿主侧调用点保持同包名兼容；`:feature-reader:testDebugUnitTest --tests "com.novel.page.read.viewmodel.PageFlipEffectTest" --tests "com.novel.page.read.service.common.ReaderServiceConfigTest"` 与 `:app:testDebugUnitTest --no-daemon` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `feature-reader` 的下一组状态层或调度实现切口 |
| 2026-03-24 | Phase 5 | 进展 | 将 `OptimizedDispatcherProvider` 迁入 `feature-reader` | 在 `DispatcherProvider` 接口层与 `ReaderServiceConfig` 迁入共享/reader 模块后，`OptimizedDispatcherProvider` 继续留在 `app` 已无必要；它本质上是阅读器 feature 的性能化调度实现 | `OptimizedDispatcherProvider` 与对应 JVM 测试已迁入 `android/feature-reader`，`app` 侧当前只保留 `DispatcherProvider` 兼容 typealias 入口和 DI 绑定；`:feature-reader:testDebugUnitTest --tests "com.novel.page.read.service.common.OptimizedDispatcherProviderTest" --no-daemon` 与 `:app:testDebugUnitTest --no-daemon` 当前通过 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续评估 `ReaderStateAdapter / ReaderReducer` 或首页契约层的下一组切口 |
| 2026-03-18 | Phase 3 | 进展 | `AuthorService` 的作家状态与注册主路径已迁入 `NetworkFacade` 主通路 | `AuthorService` 调用面比 `BookService` 更集中，且 `getAuthorStatusBlocking/registerAuthorBlocking` 已被 `NavigationBridgeModule` 真实使用，适合作为下一组高风险服务层切口 | `V3-01` 已从“Bridge + Home + Search + User + AI”推进到进一步纳入 `AuthorService` 核心路径，`AuthorServiceTest` 与 `app:testDebugUnitTest` 已通过；后续高风险网络主通路主要剩 `BookService` 与 `AuthorService` 的剩余接口 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步判断是继续拆 `AuthorService` 其余路径，还是转向更大的 `BookService` |
| 2026-03-17 | Phase 3 | 进展 | `StorageFacade` 兼容层骨架已落地 | `P3.6` 需要先把 `NovelUserDefaults` 与原始字符串 key 访问统一收口，再逐步替换业务调用；先提交兼容层骨架与单测，可以为后续低风险 key 试点提供稳定入口 | `V3-03` 已从 `planned` 推进到 `in_progress`，`LegacyStorageFacadeTest` 与 `app:testDebugUnitTest` 已通过；后续需要选择首个低风险 key 从直接 `NovelUserDefaults`/SharedPreferences 访问迁入新抽象 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步选择 Settings 或 Search 历史类低风险 key 作为 StorageFacade 首个试点 |
| 2026-03-17 | Phase 3 | 进展 | `StorageFacade` 已完成首个低风险 key 试点 | 为了把试点风险压到最低，先选择 Settings 的 `night_mode/follow_system_theme/auto_night_mode/night_start_time/night_end_time` 五个字符串 key 收口到 `SettingsPreferenceStorage`，不触碰业务复杂度更高的搜索历史和 Reader 配置 | `V3-03` 已从“只有兼容层骨架”推进到“兼容层骨架 + Settings 低风险 key 试点”，`SettingsPreferenceStorageTest` 与 `app:testDebugUnitTest` 已通过；后续可继续迁移 Search 历史或 Reader 设置 key | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步继续挑选下一组低风险 key，或回到 Bridge 边界完成 `AppError` 首批覆盖 |
| 2026-03-17 | Phase 3 | 进展 | `StorageFacade` 已扩展到 Search 历史存储 | Search 历史与历史展开状态同样是低风险、纯本地字符串 key，适合作为第二组试点；通过单独的 `SearchPreferenceStorage` 收口，可以避免直接在 `SearchRepository` 中继续散落原始 key 常量 | `V3-03` 已从“Settings 试点”推进到“Settings + Search 两组低风险 key 试点”，`SearchPreferenceStorageTest` 与 `app:testDebugUnitTest` 已通过；后续可继续迁移 Reader 相关设置或保持现状转向其他 Phase 3 主线 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步评估 Reader 低风险 key 是否适合继续迁移，否则回到 Bridge 边界完善 `AppError` |
| 2026-03-17 | Phase 3 | 进展 | `StorageFacade` 已扩展到 Reader 设置存储 | Reader 设置使用的 `PAGE_FLIP_EFFECT/FONT_SIZE/BRIGHTNESS/BACKGROUND_COLOR/TEXT_COLOR` 都是低风险本地配置 key，适合作为第三组试点；通过 `ReaderSettingsStorage` 收口后，可以不改变阅读器行为就完成设置读写抽象统一 | `V3-03` 已从“Settings + Search”推进到“Settings + Search + Reader”三组低风险 key 试点，`ReaderSettingsStorageTest` 与 `app:testDebugUnitTest` 已通过；后续可评估是否开始 `DataStore` 试点 | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步在 Phase 3 中决定优先启动 `DataStore` 试点，还是继续扩大 `AppError` 到 Bridge 边界 |
| 2026-03-17 | Phase 3 | 进展 | `DataStore` 试点已启动并通过首轮兼容迁移验证 | `P3.7` 需要一个最小、低风险、可回滚的迁移样本；先选择 Settings 的 `follow_system_theme/auto_night_mode` 两个布尔 key，通过独立 `SettingsDataStorePilot` 完成兼容迁移验证，而不立即大面积切换生产读取来源 | `V3-03` 已从“只有 StorageFacade 兼容层试点”推进到“StorageFacade + DataStore 首个迁移样本”，`SettingsDataStorePilotTest` 与 `app:testDebugUnitTest` 已通过；后续可以评估是否把 DataStore 试点扩展到更多 Settings key | 当前重构实施者 | 模块代码评审者 | 阶段门禁批准者 | 下一步决定是否继续扩大 DataStore 试点，或回到 Bridge 边界继续完善 `AppError` |
